사용 AI 도구: GitHub Copilot (Raptor mini), Python, VS Code

구현 메모:
- `solution.py`는 `첨부/조항데이터.json`에서 키워드를 검색하여 `자동화` 폴더에 결과 CSV를 저장합니다.
- `index.html`과 `styles.css`를 수정해 상단 타이틀을 한 줄로 유지하고 우측 히어로 이미지를 제거했습니다.
- `script.js`는 페이지 내 검색 기능을 지원하며, 전체 조항을 카드 형태로 렌더링합니다.
- `.gitignore`에 `자동화/`를 추가해 자동 생성된 결과 파일을 Git 추적 대상에서 제외했습니다.
